INSTALLATION INSTRUCTIONS:

0. Install numpy and nose.

1. Install the CUDA SDK.

2. Run either:
  * `pip install ./` in the root directory 
  * `make` in the `cudamat` subdirectory

3. Make sure everything works by running the test cases:

   python test/test_cudamat.py
   python test/test_learn.py
